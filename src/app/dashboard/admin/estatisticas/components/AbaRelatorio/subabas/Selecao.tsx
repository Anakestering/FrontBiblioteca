'use client';

// TODO: Sub-aba com checkboxes de seções e filtros por seção
export function Selecao() {
  return (
    <div className="card p-12 flex items-center justify-center">
      <p className="text-sm text-[var(--text-muted)]">Em breve — Seleção de seções e filtros</p>
    </div>
  );
}
