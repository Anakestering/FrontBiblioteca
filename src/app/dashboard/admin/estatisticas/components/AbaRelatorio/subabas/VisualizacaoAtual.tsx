'use client';

// TODO: Sub-aba que exporta o estado atual das outras abas (filtros já aplicados)
export function VisualizacaoAtual() {
  return (
    <div className="card p-12 flex items-center justify-center">
      <p className="text-sm text-[var(--text-muted)]">Em breve — Exportar o que está na tela</p>
    </div>
  );
}
